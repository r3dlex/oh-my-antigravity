description = "Check rate-limit state and manage auto-resume behavior for blocked Gemini work."
prompt = """
Run the oh-my-antigravity wait skill for rate-limit management.

If args are provided:
  oh-my-antigravity skill wait {{args}}
  # alias: omg skill wait {{args}}
Else:
  oh-my-antigravity skill wait
  # alias: omg skill wait

Reports current rate-limit status, auto-resume state, and recommended next action. Supports --start and --stop flags.
Report the skill output to the user.
"""
