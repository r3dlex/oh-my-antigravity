description = "Execute a concrete implementation task with progress updates and validation evidence."
prompt = """
Run the oh-my-antigravity execute skill for immediate task implementation.

If args are provided:
  oh-my-antigravity skill execute {{args}}
  # alias: omg skill execute {{args}}
Else:
  oh-my-antigravity skill execute
  # alias: omg skill execute

Makes reasonable assumptions, prefers minimal coherent changes, and reports objective, changed files, commands run, and result.
Report the skill output to the user.
"""
