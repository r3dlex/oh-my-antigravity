description = "Produce a structured handoff document summarizing completed work and next steps."
prompt = """
Run the oh-my-antigravity handoff skill to create a context transfer document.

If args are provided:
  oh-my-antigravity skill handoff {{args}}
  # alias: omg skill handoff {{args}}
Else:
  oh-my-antigravity skill handoff
  # alias: omg skill handoff

Captures completed work, current state, pending tasks, blockers, and recommended next actions.
Report the skill output to the user.
"""
