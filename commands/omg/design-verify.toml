description = "Verify code consistency with DESIGN.md tokens."
prompt = """
Run oh-my-antigravity design verify to check token consistency.

If args are provided:
  oh-my-antigravity design verify {{args}}
Else:
  oh-my-antigravity design verify

Checks that UI code uses the design tokens defined in DESIGN.md.
Use --json for machine-readable output.
Report the results to the user.
"""
