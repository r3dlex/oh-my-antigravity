description = "Run oh-my-antigravity setup with project scope (or custom args)."
prompt = """
Run oh-my-antigravity setup.

Post-global-install contract:
  after `npm install -g oh-my-antigravity`, run `omg setup --scope project`
  (equivalent: `oh-my-antigravity setup --scope project`).

If args are provided:
  oh-my-antigravity setup {{args}}
  # alias: omg setup {{args}}
Else:
  oh-my-antigravity setup --scope project
  # contributor fallback from repo root: npm run setup

Then ensure subagents catalog is present:
  - if `oh-my-antigravity setup` created `.gemini/agents/catalog.json`, continue.
  - contributor fallback from repo root: npm run setup:subagents

After running, summarize:
1) resolved scope,
2) changed/unchanged status,
3) managed files touched (including `.gemini/agents/catalog.json`).
"""
