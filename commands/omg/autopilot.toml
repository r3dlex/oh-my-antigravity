description = "Autonomously drive a feature from objective through implementation and verification."
prompt = """
Run the oh-my-antigravity autopilot skill for end-to-end autonomous execution.

If args are provided:
  oh-my-antigravity skill autopilot {{args}}
  # alias: omg skill autopilot {{args}}
Else:
  oh-my-antigravity skill autopilot
  # alias: omg skill autopilot

Autopilot will clarify the objective, produce a plan, execute it, validate with tests, and return a completion summary.
Report the skill output to the user.
"""
