description = "Run oh-my-antigravity MCP stdio server with tools/resources/prompts surfaces."
prompt = """
Run the oh-my-antigravity MCP server.

If args are provided:
  oh-my-antigravity mcp serve {{args}}
  # alias: omg mcp serve {{args}}
Else:
  oh-my-antigravity mcp serve --dry-run --json

Report:
1) resolved tool/resource/prompt surfaces,
2) whether stdio transport was started,
3) next command(s) for MCP client integration.
"""
