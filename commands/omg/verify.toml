description = "Verify that acceptance criteria are met and work is complete."
prompt = """
Run the oh-my-antigravity verify skill to check completion against acceptance criteria.

If args are provided:
  oh-my-antigravity skill verify {{args}}
  # alias: omg skill verify {{args}}
Else:
  oh-my-antigravity skill verify
  # alias: omg skill verify

Runs build checks, test suite, state artifact inspection, and criteria mapping. Reports pass/fail with evidence.
Report the skill output to the user.
"""
