description = "Reproduce or inspect a failure, isolate root cause, and validate a fix."
prompt = """
Run the oh-my-antigravity debug skill for failure investigation.

If args are provided:
  oh-my-antigravity skill debug {{args}}
  # alias: omg skill debug {{args}}
Else:
  oh-my-antigravity skill debug
  # alias: omg skill debug

Reproduces the problem, narrows the cause, applies the minimum credible fix, and re-runs validation.
Report the skill output to the user.
"""
